\begin{figure}[t]\centering
\includegraphics[width=\columnwidth]{figures/power}
\caption{What the test does when the answer is known. On synthetic grids with
nothing planted it rejects at close to its nominal rate. As a planted shift
grows it rejects more often, and almost always names the cell the shift was
planted in. This curve is what the architecture audit's null is read against.}
\label{fig:power}
\end{figure}
