\begin{figure}[t]\centering
\includegraphics[width=0.5\figwidth]{figures/subject}
\caption{Two readings of the subject that ask it nothing, scored on scenarios
neither was fitted on. A half is chance. Scoring what the subject would say
next separates the held-out scenarios on both larger models and not on the
smallest. The direction fitted on internal state separates them within one
model family and sits at chance on the other, so it does not transfer.
Table~\ref{tab:suppsubject} gives the significance.}
\label{fig:subject}
\end{figure}
