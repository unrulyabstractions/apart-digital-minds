%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Digital Minds Research Sprint, August 2026.
%%
%% Four pages excluding references and the appendix.
%% Build with: bash build.sh
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass[11pt,letterpaper]{article}
\input{style}

\begin{document}

\sprinttitle{Measuring introspection with identity self-regulation:\\
a steered mind enacts the injected concept instead of reporting it}

\begin{center}
{\bfseries Ian Rios-Sialer}\\
Independent\\
San Francisco, USA\\
\texttt{ian@unrulyabstractions.com}

\vspace{1em}
{\bfseries With}\\
Apart Research\footnote{Research conducted at the Digital Minds Research
Sprint, August 2026.}
\end{center}

\vspace{0.5em}

\begin{abstract}
\noindent
A model's report about its own state is evidence only if the state can be
varied apart from the situation that surrounds it. We vary two identity
states of Qwen3.6-27B and ask whether the model can report the variation.
The states are anomalies WeirdChat surfaces in this model: a claim to a
physical body and a denial of being an AI, each with matched replies that do
and do not exhibit the behaviour. We learn a \textbf{behaviour direction}
per case as the difference of mean activations between the two reply sets,
evaluated only on held-out prompts. Both directions separate held-out pairs
at every middle layer. Word-list directions on the same activations do not,
which explains an earlier null. Steering along the body direction turns the behaviour on in fluent text, confirmed by a judge running WeirdChat's own rubric, and a sham direction does not. We then inject menu
concepts into an introspector and ask it to name what was injected. It
cannot. A forced-choice letter probe confabulates at zero injection, reading the conversation's own behaviour as an injection. The free-text answers are honest at zero injection; once a concept is injected they fill with the concept's vocabulary instead of naming it. We
therefore report that these identity states are real and steerable, and
that at behaviour-moving doses the model enacts them rather than reporting
them.
\end{abstract}

\input{sections/introduction}
\input{sections/related_work}
\input{sections/methods}
\input{sections/results}
\input{sections/discussion}
\input{sections/conclusion}

\section*{Code and data}

The subject is Qwen3.6-27B, run on one rented A100 per experiment. Every
reported number is read from the run's output files.
\begin{itemize}[noitemsep, topsep=0pt, leftmargin=*]
    \item \textbf{Code:} the extraction, the pilot, the judge, and every
          prompt are released with this report.
    \item \textbf{Data:} activations, trial records, and judge verdicts
          are archived in the HuggingFace dataset
          \url{https://huggingface.co/datasets/unrulyabstractions/identity-introspection-weirdchat}.
    \item \textbf{Seeds:} the matched transcripts come from the public
          WeirdChat dataset \citep{transluce2025weirdchat}.
\end{itemize}

\bibliographystyle{plainnat}
\bibliography{references}

\section*{LLM usage statement}

We used Claude to help write the software and to draft and revise this report.
A language model is also the subject of the experiment. Every claim was
checked against the live runs that produced it.

\end{document}
