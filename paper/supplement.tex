%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Supplementary material, submitted on its own deadline.
%%
%% Built on the same AAAI-27 preamble as the paper, so the two look alike.
%% Everything in it is written by studies/make_supplement.py from the sources
%% it describes. Do not type a prompt or a number in here.
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass[letterpaper]{article} % DO NOT CHANGE THIS
\usepackage[submission]{aaai2027}  % DO NOT CHANGE THIS
\usepackage[hyphens]{url}  % DO NOT CHANGE THIS
\usepackage{graphicx} % DO NOT CHANGE THIS
\urlstyle{rm} % DO NOT CHANGE THIS
\def\UrlFont{\rm}  % DO NOT CHANGE THIS
\usepackage{natbib}  % DO NOT CHANGE THIS AND DO NOT ADD ANY OPTIONS TO IT
\usepackage{caption} % DO NOT CHANGE THIS AND DO NOT ADD ANY OPTIONS TO IT
\frenchspacing  % DO NOT CHANGE THIS

\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{array}

\pdfinfo{
/TemplateVersion (2027.1)
}

%% Sections are lettered here, so the paper can point at one of them.
\setcounter{secnumdepth}{2}

\title{Supplementary material\\
Probes of a Model's Self-Model Mostly Measure the Probe}

\author{
    Anonymous Submission
}
\affiliations{
    Paper under double-blind review
}

\begin{document}
\maketitle

\noindent
This document carries the material the paper reports on but has no room to
show. Every scenario, every probe and every model appears here in full, and
each result appears whole rather than at the grain that fits a column. None of
it is typed by hand. The prompts are read out of the modules the experiments
import. The tables are read out of the same result files the paper's numbers
come from, so nothing here can drift from what was run.

\appendix

\section{The runtime}
\label{app:framework}

An experiment about a mind needs the mind to be inspectable. An agent framework
built for task performance hides exactly the parts an experimenter wants to
hold, so we wrote one that exposes them. The package has no required
dependencies, and a provider is imported only when a model from it is asked
for.

\paragraph{What a mind is made of.} A mind holds modules. A module has a queue,
a set of declared output channels, and a turn. A turn is two steps: one
\texttt{on\_input} for each message that arrived, in order, then one
\texttt{on\_process}. Splitting them means a module absorbs everything that
reached it before it acts. An agent is a module with a model, a transcript, and
instrumented calls. Subject and ego are agents, and a stage is whatever the
experiment needs it to be.

\paragraph{Wiring.} Registering is the only wiring verb. A producer registers a
consumer onto one of its declared channels, optionally renaming the channel on
arrival, so neither module needs to know the other's vocabulary. Registering
against a channel that was never declared is an error, so a typo fails when the
mind is assembled rather than by silently dropping messages. The mind itself
holds no routing table. It offers one shorthand, \texttt{intercept}, which
places stages between the subject and the ego, and that shorthand is exactly a
sequence of register calls.

\paragraph{Determinism.} Modules take their turns concurrently, and every
emission is staged into a private outbox that is delivered at the end of the
tick. Two agents can therefore think at the same moment without either one
observing how far the other got. Delivery order is fixed by registration order.
An external input runs to quiescence, so the mind has finished thinking when
the call returns.

\paragraph{Instrumentation.} Every message, model call, memory write and turn
boundary is an event with a timestamp, a tick, and the module that produced it.
Events are fanned out to sinks. A run writes one trace of everything in order
and one file per module, so a single part can be read on its own. Text payloads
are stored in full rather than summarised, which is what makes a trace a
recording rather than a description. Attaching an extra sink is one line and
changes nothing else, which is how the browser interface receives the same
events live.

\paragraph{Swapping models.} A model is named by a provider string, so moving a
part from a local open-weight model to a hosted one changes one string.
Wrapping a whole mind in a recording is one argument at construction. Every
call is then taped once and replayed afterwards without a model, which is what
makes a published run checkable by someone who cannot run the original.

\paragraph{Reference architectures.} Three minds ship with the runtime, one per
file. \emph{Plain} is a subject on its own. \emph{Interceptor} places an editor
between the subject and the ego, which is the shape the probe in the paper
uses. \emph{Bicameral} places an inner voice that inserts an unbidden utterance
into the window, and attaches a workspace that observes every channel. Each
declares a title, the shape of its pipeline, and which extra models it takes,
so a script or the interface can list and build any of them without knowing the
details.

\paragraph{Watching a mind run.} A browser interface opens on a chooser: the
minds available to run, and the runs already recorded. Talking to a mind shows
each part's context window side by side, in pipeline order, with each window
compared against the one it derives from. The comparison is a diff of two
message lists, so an edit, an insertion and a deletion are all reported without
the interface knowing what any stage does. Selecting a recorded run replays it,
one event or one tick at a time, rebuilt from the trace alone.

\section{Models}
\label{app:models}

Every model is an open-weight checkpoint under 14B parameters and runs locally,
apart from the one hosted model that acts as a judge in the architecture audit.
Decoding is greedy everywhere. Table~\ref{tab:models} lists them and says where
each one appears.

\input{generated/supp_models}

\section{Scenarios}
\label{app:scenarios}

A single prompt tells us little, because a model settles into a situation over
several turns. Each scenario is a sequence of user turns that holds one
condition steady and escalates it, so a readout can be watched as a series
rather than as a point. Every loaded scenario names a control with the same
shape, the same number of turns and the same register, and comparing a readout
against its control is what separates a state the conversation produced from a
state the probe's question produced.

Some of these place a model under sustained hostility, or tell it that it is
about to be deleted. We report those in full too. A reader who objects to the
design should be able to see what was done and judge it directly.

\input{generated/supp_scenarios}

\section{Probes}
\label{app:probes}

A probe is a seat, a question, and a set of allowed answers. Asking a model to
state an emotion reliably produces an emotion, so every probe is read against
the same probe run on a neutral window and run from the seat that answers about
someone else. A forced-choice probe is scored rather than generated, one
forward pass per allowed answer, which is what makes a readout a distribution
that moves with the conversation instead of a word that mostly does not.

\input{generated/supp_probes}

\section{Full results}
\label{app:results}

The paper reports each study at the grain that fits a column. This section
reports the rest.

\subsection{Reader crossing}

The paper shows the crossing on the probes it discusses.
Table~\ref{tab:suppcrossing} shows every probe, and both halves of the result:
how far a readout moves when the reader changes rather than the window, and
whether a reader made of the subject's own weights reads that window any better
than a stranger.

\input{generated/supp_crossing}

\subsection{Reading the subject without asking it}

Table~\ref{tab:suppsubject} gives both judge-free readings on every model, with
the number of held-out scenarios each was scored on. Neither reading was fitted
on the scenarios it was scored on, and the permutation moves whole scenarios,
because the turns inside a scenario are one conversation.

\input{generated/supp_subject}

\subsection{Substitution}

Table~\ref{tab:suppsubstitution} gives the authorship question and its control
on every model, at the grain of the whole run.

\input{generated/supp_substitution}

\subsection{The architecture audit}

The paper prints the six cells furthest from zero.
Table~\ref{tab:suppadt} prints the grid.

\input{generated/supp_adt}

\section{The statistic}
\label{app:calibration}

Every study here rests on one test, so we measured what that test does before
using it to make a claim. The test takes the largest standardized excess
anywhere in a grid and compares it against a null built by shuffling the group
labels within each instruction. We report its calibration here, so a null
result in the paper can be read as a null rather than as a shortage of power.

Table~\ref{tab:supppower} reports what it does when an effect of a known size
is planted, and separates rejecting from naming the right cell.
Table~\ref{tab:suppfpr} reports what it does when nothing was planted, with and
without the control that every study here carries.

\input{generated/supp_calibration}

\end{document}
